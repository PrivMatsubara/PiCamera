version: '3.8'

services:
  web:
    build: .
    container_name: picamera-web
    ports:
      - "5000:5000"
    devices:
      # カメラデバイスのマウント（必要な構成に応じて /dev/video0 等に変更してください）
      - "/dev/video0:/dev/video0"
    restart: always
